module DataStructures{

    test Person = (23, "test")

    data Person{
        age Int
        name String
    }

    fun main() : Int {
        print({test.age})

        PersAlt (Int, String) = (18, "alternate")
        print({persAlt.name})
        return 0
    }
}