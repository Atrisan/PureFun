module DataStructures{

    list [Int] = [1,2,3,4]

    fun main() : Int {
        for i Int = 0; i < 4; i += 1 {
            Int res += 1
        }

        for l in list {
            print({l})
        }

        x Int = 0
        while x < 4{
            x++
        }
        return 0
    }
}